{
  "name": "bb-plugin-machine-ssh",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Add machines you can reach with SSH. Cloudroom installs itself using your SSH keys.",
  "engines": {
    "bb": ">=0.0"
  },
  "bb": {
    "name": "SSH Machines",
    "description": "Add machines you can reach with SSH. Cloudroom installs itself using your SSH keys.",
    "branding": {
      "icon": "Terminal"
    },
    "server": "./server.ts"
  },
  "keywords": [
    "bb-plugin"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "prepare:bundled": "bb-plugin-build prepare-bundled"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@bb/plugin-build": "workspace:*",
    "@get-bb/plugin-sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "npm:@typescript/typescript6@^6.0.2"
  }
}
